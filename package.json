{
  "name": "claude-channel-setup",
  "version": "0.1.0",
  "description": "Automated setup for Claude Code channel plugins (Discord, Telegram)",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "bin": {
    "claude-channel-setup": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:build": "tsc --noEmit && npm run build"
  },
  "keywords": ["claude", "claude-code", "discord", "telegram", "bot", "setup", "cli"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "ora": "^8.0.0",
    "picocolors": "^1.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
